[project]
name = "pinky-camera-server"
version = "0.1.0"
requires-python = ">=3.9"
description = "Camera server for Pinky robot (Python side of IPC camera bridge)"
dependencies = [
    "pyzmq>=25.0",
    "opencv-python>=4.9.0",
    "numpy>=1.24.0",
]
